/**
 * Theme switcher copy for `docs/design-system.md` §5.1. Shared chrome rather than
 * a page: the switcher sits in the application header on every surface.
 */
export const theme = {
  group: "Theme",
  system: "System",
  light: "Light",
  dark: "Dark",
} as const;
