/**
 * Application shell copy — the chrome that wraps every signed in page, so it is a
 * shared surface rather than one page's module.
 */
export const shell = {
  skipToContent: "Skip to content",
  nav: {
    label: "Main",
    creatorHome: "Home",
    series: "Series",
  },
} as const;
